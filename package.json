{
  "name": "model_reducer",
  "version": "0.0.1",
  "description": "A reducer based on a Model structure",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/istanbul test test/main.js",
    "coveralls": "./node_modules/.bin/istanbul cover test/main.js && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "lint": "./node_modules/.bin/eslint -c .eslintrc.json ./lib/ ./test/",
    "test_no_cov": "node test/main.js"
  },
  "keywords": [
    "reducer"
  ],
  "files": [
    "lib/"
  ],
  "author": "Duncan McDougall",
  "private": true,
  "devDependencies": {
    "coveralls": "^2.11.15",
    "eslint": "^3.5.0",
    "istanbul": "^0.4.5",
    "jasmine": "^2.5.1"
  }
}
